# Reticulum Node Manager

Unified process manager for Reticulum mesh networking nodes with FreeDV TNC2 and Direwolf.

## Quick Start

```bash
pip install reticulum-node-manager
rnm check          # verify dependencies
rnm setup           # interactive configuration
rnm start           # launch all services
```
